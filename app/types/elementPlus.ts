/** Element Plus 尺寸 */
export type ElementPlusSize = 'default' | 'small' | 'large'

/** Element Plus 信息类型（用于 message、tag 等） */
export type ElementPlusInfoType = 'success' | 'info' | 'warning' | 'danger'
